{
  "collectionName": "components_shared_search_content_types",
  "info": {
    "displayName": "Search Content Type",
    "description": "Controls which content type and fields are included in site search"
  },
  "attributes": {
    "contentType": {
      "type": "enumeration",
      "enum": ["pages", "blog-posts", "press-releases", "events"],
      "required": true
    },
    "label": {
      "type": "string",
      "description": "Display label shown in filter chips (e.g. Pages, Blog Posts)"
    },
    "enabled": {
      "type": "boolean",
      "default": true
    },
    "fields": {
      "type": "text",
      "description": "Comma-separated field names to search (e.g. title,content,subtitle). Leave blank for defaults."
    }
  }
}
